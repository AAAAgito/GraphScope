g.timeout(600000).V().hasLabel('person').has('id', $personId).out('knows').union(identity(),out('knows')).dedup().has('id',neq($personId)).as('friends').in('hasCreator').has('creationDate',lt($maxDate)).as('post').order().by('creationDate', desc).by('id', asc).limit(20).select('friends','post').by(valueMap('id', 'firstName', 'lastName')).by(valueMap('id', 'content', 'imageFile', 'creationDate'))